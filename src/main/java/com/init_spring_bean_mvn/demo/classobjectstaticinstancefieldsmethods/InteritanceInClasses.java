package com.init_spring_bean_mvn.demo.classobjectstaticinstancefieldsmethods;

public class InteritanceInClasses {

    // When do you use? You actually already do - java.lang.Object - link to java's api for class
    // java.base/java/lang/Object.html - every class has object as superclass - all objects , including arrays
        // methods - clone, equals, toString() - every class we create inherits these methods from object class - we can override them to provide our own implementation

        // string class has over 60 methods - all of these methods are inherited from object class - we can use them in our string objects

    // Can override toString() in subclass of a custom class
    // Can't extend more than one class - hierarchy of inheritance is cumulative -all classes can use or override objects methods



    // Class diagram - allows us to design our classes before we build them

}
